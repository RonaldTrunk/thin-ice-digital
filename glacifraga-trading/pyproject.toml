[project]
name = "glacifraga-trading"
version = "0.1.0"
description = "Institutional breakout signals with ATR risk and an explainable REST API."
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
